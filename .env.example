# Server Configuration
PORT=4318
LOG_LEVEL=info

# SQLite Database Configuration
# This path is relative to packages/backend/src directory, or can be an absolute path.
# Default will resolve to the root data/ folder.
DB_PATH=../../data/otel_ai.db
